# ── Application ──
PORT=3001
JWT_SECRET=your-secret-key-change-in-prod
SUPER_ADMIN_EMAIL=contact@breaktopia.io

# ── SMTP (Proton Mail) ──
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=cedric.p@breaktopia.io
SMTP_PASS=your-smtp-token-here
MAIL_FROM="Breaktopia <contact@breaktopia.io>"

# ── Mode ──
# Set to "production" to enable real email sending
# In development, emails are logged to console instead
NODE_ENV=development
